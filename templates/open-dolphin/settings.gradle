
new File(".").eachDir() { dir ->
	if ( ! ['gradle'].contains(dir.name) && !dir.name.startsWith('.')) {
		println "*** \${dir.name}"
		include "\${dir.name}"
	}
}
rootProject.name = '${project_name}'

