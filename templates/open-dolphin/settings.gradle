
new File(".").eachDir() { dir ->
	if ( ! ['gradle'].contains(dir.name) && ! ['.', 'org-'].any { it.startsWith(it) } {
		println "*** \${dir.name}"
		include "\${dir.name}"
	}
}
rootProject.name = '${project_name}'

