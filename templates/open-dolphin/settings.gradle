
new File(".").eachDir() { dir ->
	if ( ! ['gradle', 'build'].contains(dir.name) && ! ['.', 'org-'].any { dir.name.startsWith(it) } ) {
		println "*** \${dir.name}"
		include "\${dir.name}"
	}
}
rootProject.name = '${project_name}'

